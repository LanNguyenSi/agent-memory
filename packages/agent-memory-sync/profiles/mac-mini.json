{
  "//": [
    "Machine profile: Mac mini — single source of truth. The bare git repo",
    "referenced by remoteUrl lives ON this machine (see",
    "docs/machine-setup.md for the `git init --bare` bootstrap); this profile",
    "is what runs the scheduled `run --mode sync` / `watch` on the mini",
    "itself, reaching its own bare repo over the same `mini` SSH alias other",
    "machines use (keeps the command line identical across machines — only",
    "the profile file differs). A plain local path also works if you prefer",
    "not to route the mini's own traffic through ssh to itself; see the",
    "commented alternative below.",
    "",
    "Namespace divergence (read before copying this file to another machine):",
    "Claude Code derives its per-project memory slug from the absolute path",
    "of the project working directory, including the OS username. On the",
    "mini the repo lives at /Users/lannguyensi/git/pandora, so Claude Code",
    "stores memory under",
    "~/.claude/projects/-Users-lannguyensi-git-pandora/memory — hence the",
    "hardcoded rootDir below. On the MacBook the OS user differs (lan), so",
    "the same repository checkout path pattern yields a DIFFERENT slug:",
    "-Users-lan-git-pandora (see macbook.json). The two machines' local",
    "memory directories are intentionally NOT named the same; only the",
    "remote (repositorySubdir 'mac-mini') and the shared bare repo are",
    "common ground.",
    "",
    "rootDir intentionally uses a resolved absolute path rather than '~':",
    "agent-memory-sync's config loader does not perform shell-style tilde",
    "expansion (path.resolve('~/x') resolves '~' as a literal path segment,",
    "not the home directory) — see src/config/loader.ts resolveRunConfig().",
    "Adjust the /Users/lannguyensi prefix if you copy this profile to a",
    "machine with a different OS username.",
    "",
    "Activation: pass BOTH --config (this file) and the 'mac-mini' profile",
    "argument, e.g. `agent-memory-sync run mac-mini --config profiles/mac-mini.json`.",
    "The CLI's [profile] positional argument always defaults to 'default' and",
    "overrides this file's 'profile' field when omitted, so state files",
    "(queue/, base/, tmp/) land under the intended profile subdirectory only",
    "if the positional argument is passed explicitly too. See docs/machine-setup.md.",
    "",
    "Alternative remoteUrl if you want the mini to write to its own bare repo",
    "directly instead of looping through ssh to itself:",
    "  \"remoteUrl\": \"/Users/lannguyensi/memory-sync/pandora-memory.git\""
  ],
  "profile": "mac-mini",
  "rootDir": "/Users/lannguyensi/.claude/projects/-Users-lannguyensi-git-pandora/memory",
  "remoteUrl": "mini:~/memory-sync/pandora-memory.git",
  "branch": "main",
  "repositorySubdir": "mac-mini",
  "conflictStrategy": "inline-markers",
  "reachabilityTimeoutMs": 5000,
  "syncPaths": [
    { "source": "MEMORY.md", "destination": "MEMORY.md", "kind": "file" },
    { "source": "daily", "destination": "daily", "kind": "directory" },
    { "source": "logs", "destination": "logs", "kind": "directory" }
  ]
}
