---
name: Legacy feedback without new fields
description: Pre-router memory — no topics/severity/triggers
type: feedback
---

Some legacy guidance that lives in a memory file written before the router
shipped. Must still load cleanly, just can't fire via topic or tool gates
without the new frontmatter.
