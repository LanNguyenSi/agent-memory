#!/usr/bin/env node
/**
 * MCP server stub — wires memory_search, memory_apply, memory_resolve tools.
 * Actual MCP SDK integration lands in a follow-up task once the Gate layer
 * is validated. For now this file is a placeholder so the package `bin`
 * entry resolves and the scaffold is complete.
 */
process.stderr.write(
  'memory-router MCP server not yet implemented — see follow-up task.\n',
);
process.exit(2);
